# RAIDJ リスク・制約事項メモ

---

## 1. 外部サービス依存リスク

### yt-dlp（YouTube 読み込み）
| リスク | 内容 |
|---|---|
| 発生頻度 | 高（YouTube は定期的に仕様変更する） |
| 影響範囲 | YouTube 統合機能が完全停止 |
| 対策 | 起動時に自動更新を実装済み。ただし yt-dlp 側の対応に数日〜数週間かかる場合がある |
| 緊急時 | yt-dlp を手動で最新版に更新（`pip install -U yt-dlp`）|

### Google Drive API
| リスク | 内容 |
|---|---|
| 無料枠上限 | Google Drive の無料容量は 15GB。楽曲ファイルをバックアップすると超過しやすい |
| API 変更 | Google が API v3 を廃止・変更した場合、認証や操作が壊れる可能性 |
| 対策 | バックアップ対象に楽曲ファイル本体は「任意」設定にし、DB・設定ファイルを優先 |

---

## 2. ライブラリ・ソースコード互換性リスク

### Mixxx 2.6 と追加ライブラリの互換性

| 組み合わせ | リスク | 対策 |
|---|---|---|
| **Essentia（AGPL v3）× Mixxx（GPL v2）** | 配布する場合はライセンス非互換。個人利用のため現時点では問題なし | 個人利用の範囲を維持する |
| **FFmpeg（LGPL/GPL）× Mixxx** | GPL コーデック（libx264 等）を有効にすると GPL 汚染になる | vcpkg の FFmpeg を LGPL 構成でビルド（デフォルト）のまま使用 |
| **NDI SDK × Windows MSVC** | 公式 Windows バイナリあり。MSVC との ABI 互換は保証済み | バージョンを固定して使用 |
| **ONNX Runtime × Qt6 スレッド** | ONNX の推論スレッドと Qt のオーディオスレッドが競合する可能性 | ONNX 推論を専用スレッドプールで実行し、オーディオスレッドとは分離する |
| **Demucs ONNX モデル × ONNX Runtime バージョン** | モデルをエクスポートした ONNX バージョンと Runtime のバージョンが合わない場合に実行エラー | Mixxx GSoC 2025 成果物と同じ ONNX Runtime バージョンを vcpkg で固定 |

### ⚠️ SQLCipher × Mixxx SQLite3（解決済み）
| 内容 | SQLCipher と SQLite3 は同一シンボルを持つため同時リンク不可 |
|---|---|
| 解決策 | `qsqlcipher-qt6` プラグインで Qt の QSQLITE ドライバーを差し替え |
| 効果 | 暗号化なし DB（mixxxdb）も暗号化あり DB（Rekordbox）も同一ドライバーで扱える |
| ビルド手順 | `BUILD_SETUP.md` Step 4 参照 |

### ⚠️ Essentia × FFmpeg バージョン（スタティックビルドで回避済み）
| 内容 | Essentia 内部の FFmpeg と Mixxx の FFmpeg が競合する可能性 |
|---|---|
| 解決策 | Essentia を `--static` ビルド。FFmpeg を Essentia 内部に静的リンクして分離 |
| ビルド手順 | `BUILD_SETUP.md` Step 5 参照（スタティックビルド指定済み） |

### Mixxx 内部 API の変更リスク

| 箇所 | リスク |
|---|---|
| `LibraryFeature` クラス | YouTube・Rekordbox 機能の追加点。Mixxx 本家が内部 API を変更すると実装箇所が壊れる |
| `AnalyzerManager` クラス | Essentia・Demucs を追加する箇所。解析パイプラインの変更に引きずられる可能性 |
| `EffectsManager` クラス | エフェクト追加・VST3 対応の追加点 |
| Qt6 → Qt7 移行 | 将来 Mixxx が Qt7 に移行した際に QML・ウィジェット API の破壊的変更が起こりうる |

**方針：** 本家追従はしないため、Mixxx 本体の変更に巻き込まれるリスクは低い。ただし致命的なバグ修正を取り込む際は手動マージが必要になる。

---

## 3. ハードウェア依存リスク

### Numark Party Mix II MIDI 仕様
| リスク | 内容 |
|---|---|
| 仕様非公開 | ライトショーの MIDI メッセージは非公開のため当初調査予定だったが対応しないことに決定 |
| ファームウェア更新 | Numark が firmware を更新した場合、MIDI 割り当てが変わる可能性がある |
| 対策 | マッピングファイルをバージョン管理し、変更があれば再調整 |

### Stems モード（パッド拡張）の制約
- Party Mix II のパッドは物理的に 4 つ。ステム数が 4（ドラム・ベース・ボーカル・その他）のためちょうど対応するが、LALAL.AI の 10 ステムモードでは UI が不足する
- **対策：** 10 ステム時は画面 UI でのみ操作可能とし、パッドは主要 4 ステムに固定

---

## 4. AI・モデル依存リスク

### Demucs v4 ONNX モデル
| リスク | 内容 |
|---|---|
| モデルサイズ | 約 300MB。初回 DL 環境によっては時間がかかる |
| CPU 負荷 | 事前分離処理中はCPU を大きく使用する。オーディオエンジンへの影響を監視する必要あり |
| モデル精度 | 特定ジャンル（ジャズ・クラシック等）で分離精度が下がる場合がある |
| 対策 | 分離処理は専用スレッドで実行。オーディオスレッドの優先度を常に最高に保つ |

### LALAL.AI API（オプション）
| リスク | 内容 |
|---|---|
| 費用 | 従量課金。使いすぎると費用がかかる |
| API 停止 | サービス停止・API 変更のリスク |
| 対策 | あくまでオプション機能とし、未設定時は Demucs にフォールバック |

---

## 5. データ管理リスク

### YouTube 一時ファイル（10GB 上限）
| リスク | 内容 |
|---|---|
| 削除タイミング | ソフトが異常終了した場合、削除処理が走らず 10GB を超過したまま残る可能性 |
| 対策 | 起動時にも容量チェックを実行し、超過分を削除する |

### Rekordbox DB（master.db）の形式変更
| リスク | 内容 |
|---|---|
| Pioneer のアップデート | Rekordbox がアップデートされると DB スキーマが変わり、インポートが壊れる可能性 |
| 対策 | pyrekordbox ライブラリの更新に追従。インポート失敗時は明確なエラーメッセージを表示 |

---

## 6. VJ 機能リスク

### FFmpeg xfade の処理負荷
| リスク | 内容 |
|---|---|
| CPU/GPU 負荷 | 映像 2 本デコード＋GLSL 合成の同時処理。低スペック PC では音声に影響が出る可能性 |
| 対策 | VJ 機能を ON/OFF できるトグルを用意。OFFの場合は映像処理を完全スキップ |

### NDI SDK のライセンス変更
| リスク | 内容 |
|---|---|
| 将来のライセンス変更 | NDI が将来有料化・条件変更する可能性 |
| 対策 | 代替として OBS への仮想デバイス（VB-Audio）経由の音声出力も案内済み |

---

## 7. 対処優先度まとめ

| 優先度 | リスク | 対処方針 |
|---|---|---|
| 高 | yt-dlp 停止 | 自動更新で対応。停止時は手動更新を案内 |
| 高 | Demucs×オーディオスレッド競合 | 専用スレッド分離で設計段階から対処 |
| 中 | Rekordbox DB スキーマ変更 | pyrekordbox の更新追従 |
| 中 | VJ の CPU 負荷 | ON/OFF トグルを必ず用意 |
| 低 | NDI SDK ライセンス変更 | 代替手段（VB-Audio）を常に用意 |
| 低 | Google Drive API 変更 | 発生時に対応 |

---

最終更新：2026-06-04
