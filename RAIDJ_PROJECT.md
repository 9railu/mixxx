# RAIDJ — プロジェクト計画書

---

## プロジェクト概要

**RAIDJ** は、Mixxx 2.6（OSS DJ ソフト）をフォーク・拡張した個人用 DJ ソフトウェアである。
Numark Party Mix II コントローラーを使い、自宅・ホームパーティーレベルで Rekordbox 並みの体験を実現することを目的とする。

- 配布・商用利用なし。完全個人利用。
- サブスクリプション不要。
- 手動 DJ を前提とし、オートミックス・AI 次曲提案は持たない。

---

## 開発の背景と動機

| 課題 | 内容 |
|---|---|
| Numark Party Mix II は Rekordbox 非対応 | Pioneer 製品でないためライセンスがなく、Performance Mode が使えない |
| Rekordbox 自体にサブスク費用がかかる | 個人利用に対してコストが見合わない |
| 既存の OSS（Mixxx）は機能が不足 | ステム分離・VJ・クラウド連携・YouTube 読み込みなどが欠ける |

→ Mixxx をベースに必要な機能を自分で実装した独自ビルドを作る。

---

## 使用コントローラー

**Numark Party Mix II**
- USB クラスコンプライアント MIDI デバイス（ドライバー不要）
- 2 デッキ・ジョグホイール・パッド（4 個）・ミキサー搭載
- 公式対応は Serato DJ Lite のみ → RAIDJ で完全対応させる

---

## 技術基盤

| 項目 | 内容 |
|---|---|
| ベースソフト | Mixxx 2.6（GPL v2・C++17 / Qt6） |
| 本家追従 | しない（必要時のみ手動マージ） |
| ビルド環境 | Windows / MSVC + vcpkg |
| 主要追加ライブラリ | Essentia（BPM/Key）・Demucs v4 ONNX（ステム分離）・FFmpeg（映像）・GL Transitions（VJ）・NDI SDK（配信）・yt-dlp（YouTube）・Google Drive API v3（バックアップ）・SQLCipher + qsqlcipher-qt6（Rekordbox DB）・OpenSSL・ONNX Runtime |

---

## 機能一覧と目的

### 1. Numark Party Mix II 完全対応
**目的：** コントローラーのすべての操作を RAIDJ で使えるようにする。

- MIDI マッピングの動作確認・ジョグホイール精度改善
- パッドモードに **Stems モード**を追加
  - パッドモード切り替えボタンで `Hot Cues → Auto Loop → Manual Loop → Sampler → Stems` を順番に切り替え
  - Stems モード時：PAD 1〜4 でドラム・ベース・ボーカル・その他を**ミュート/復元**（ミュート方式）
  - ステムミュート状態を LED の色で表示

---

### 2. Rekordbox キュー・ライブラリ連携
**目的：** 今まで Rekordbox で打ったキューやプレイリストをそのまま RAIDJ に持ち込む。

- Rekordbox ローカル DB（`master.db`）から直接インポート（pyrekordbox ベース）
- ホットキュー（色情報含む）・メモリキュー・ビートグリッドを読み込み
- Rekordbox XML・USB エクスポートにも対応
- My Tag のインポート対応

---

### 3. 起動時間の改善
**目的：** 使いたいときにすぐ使えるようにする。現状の Mixxx は初回起動に最大 1 分 40 秒かかる。

- ライブラリ読み込みを非同期化（UI を先に表示してバックグラウンドでスキャン）
- SQLite クエリの最適化・インデックス追加
- 外部ライブラリ連携（Traktor / Serato / iTunes）の遅延初期化
- yt-dlp バージョン確認・Demucs モデルの初回 DL もバックグラウンドで実行

---

### 4. BPM・キー解析の高精度化
**目的：** 解析精度を上げてハーモニックミックスやビートマッチングの精度を高める。

- 現行の Queen Mary DSP から **Essentia**（MTG Barcelona 製・学術検証済み）へ移行
- BPM：Essentia RhythmExtractor2013（変動テンポ・複雑なリズムに強い）
- キー：Essentia KeyExtractor（libkeyfinder と選択可能）
- **フレキシブル BPM グリッド**対応（ロック・ディスコ等の変動テンポ楽曲）
- 複数エンジン併用による信頼度スコアベースの最終値決定

---

### 5. 波形表示の改善
**目的：** Rekordbox と同等の視認性を実現する。

- 周波数帯域別カラー表示（低音=赤・中音=緑・高音=青など）
- 波形の高精細化・ズーム時の品質改善

---

### 6. ライブラリ機能の拡張
**目的：** 大量の楽曲を効率よく管理する。

- スマートプレイリスト（BPM・キー・ジャンル等の条件でフィルタ）
- 大規模ライブラリでの検索・スクロールの高速化

---

### 7. エフェクトの強化
**目的：** Traktor Pro 4 相当のエフェクト環境を実現する。

**フェーズ 1：C++ 直接実装**
- 主要エフェクト追加（フランジャー・フェイザー・ビットクラッシャー・リバーブ等）
- 4 スロットのエフェクトチェーン・プリセット保存
- BPM 同期エフェクト（ディレイ・フランジャー等）
- フィルター / EQ / アイソレーターの品質改善

**フェーズ 2：VST3 対応（将来）**
- 市販・フリーの VST3 プラグインをロードして使用可能にする

---

### 8. ステム分離（AI）
**目的：** 任意の楽曲をボーカル・ドラム・ベース・その他に分離してリアルタイム操作できるようにする。

使用エンジン：**Demucs v4 ONNX**（Meta AI 製・Mixxx GSoC 2025 成果を取り込み）
初回起動時に自動ダウンロード（約 300MB）。

**モード A：事前分離＋キャッシュ（ローカル楽曲）**
- 楽曲ロード時にバックグラウンドで分離処理
- 分離済みステムをキャッシュ保存 → 2 回目以降は即時ロード

**モード B：簡易リアルタイム分離（YouTube 楽曲）**
- 軽量モデル（Demucs small）を使用
- ダウンロード完了直後から分離処理開始（vocal / instrumental の 2 ステムに絞る）
- 完了後はモード A と同じキャッシュ管理へ移行

オプション：LALAL.AI API キー設定で高品質 10 ステム分離に切り替え可能。

---

### 9. YouTube 統合
**目的：** YouTube の楽曲を直接デッキにロードして DJ できるようにする。個人利用・配布なし前提。

内部処理：`yt-dlp → ffmpeg → デッキへ直接ロード`

**URL 直接入力**
- ライブラリパネルの YouTube セクションに URL を貼り付け
- メタデータ（タイトル・長さ・サムネイル）をプレビュー表示後にデッキへロード

**キーワード検索**
- yt-dlp の検索機能で結果取得（API キー不要）
- 検索結果一覧からワンクリックでデッキにロード・検索履歴保存

**ファイル管理**
- 一時ファイル上限：**10GB**（超過分はソフト終了時 / 次回起動時に古い順削除）
- **お気に入りボタン**：押したものは別フォルダに永続保存・削除対象外
- **YouTube 動画 ID をキー**に mixxxdb へキュー・ホットキューを保存
  → 一時ファイルが削除されて再ダウンロードしても同じ動画であればキューが自動復元
- yt-dlp は起動時にバックグラウンドで自動更新（YouTube 仕様変更に追従）

---

### 10. 楽曲バックアップ（Google Drive）
**目的：** PC 故障・紛失時でも設定・キュー・ライブラリをすべて復元できるようにする。

- OAuth 2.0 認証。トークンは **Windows Credential Manager（DPAPI）** で安全保存
- バックアップ対象：mixxxdb.sqlite / キュー・ビートグリッド / YouTube キャッシュDB / マッピングファイル / 設定ファイル / 楽曲ファイル本体（任意）
- 手動実行・自動実行（起動時/終了時/定期）を選択可能
- 差分バックアップ（変更ファイルのみ転送）・世代管理
- リストア機能付き
- 将来：OneDrive / Dropbox / NAS 対応も検討

---

### 11. セトリ（セットリスト）書き出し
**目的：** DJ プレイ後に曲リストを各種形式で出力・シェアできるようにする。

**記録モード（ON/OFF トグルで全体制御）**
- **自動記録**：再生開始と同時に記録
- **手動記録**：ボタンを押している区間だけ記録（自動と並行・独立して保持）
- 記録内容：曲名・アーティスト・BPM・キー・再生開始時刻・再生時間
- 書き出し時に自動 / 手動どちらのセトリを使うか選択

**書き出し形式**
- テキスト（.txt）/ CSV / JSON / 画像（.png・SNS シェア用カード）
- クリップボードへのワンクリックコピー
- Google Drive 自動保存・Twitter/X 向け文字数制限対応フォーマット

---

### 12. VJ 機能（オートVJ）
**目的：** DJ プレイをするだけで自動的にビジュアルが動くようにする。VJ 側を手動操作しなくてよい。

**自動ビジュアル（コア）**
- BPM → ビジュアルアニメーション速度に自動同期
- FFT 周波数分析 → 低音/中音/高音エネルギーをビジュアルパラメータに反映
- クロスフェーダー位置 → 映像クロスフェード率に自動連動
- ホットキュー押下 → フラッシュ・色変化・エフェクト自動発動
- ループ中 → 映像もループエフェクト自動適用

**自動シーン切り替え**
- ドロップ検出 → 激しいビジュアルへ自動遷移
- ブレイク検出 → 落ち着いたビジュアルへ自動遷移
- エネルギーレベルに応じて **GL Transitions 121 種**から自動選択
- BPM に合わせてエフェクト強度・速度を自動補正（映像が音楽に馴染む）

**映像素材**
- YouTube 楽曲ロード時 → その映像を VJ 素材として自動使用
- ローカル楽曲ロード時 → 指定フォルダからランダム再生

**映像クロスフェード**
- FFmpeg libavfilter xfade + GL Transitions GLSL シェーダーで実装
- 音声クロスフェードに映像クロスフェードを自動追従

**配信連携**
- NDI SDK → 映像＋音声を OBS へ送出
- OBS WebSocket → 曲名・BPM・キーを OBS テキストに自動反映
- VJ 出力ウィンドウ（外部ディスプレイ / プロジェクター向け）

**手動調整（必要なときだけ）**
- VJ ON/OFF トグル・トランジション手動上書き・エフェクト強度スライダー

---

### 13. UI・操作性の改善
**目的：** 操作ミスを減らし、レビューで繰り返し指摘されている既知問題を解消する。

- ビートグリッドのタイミングオフセット修正（全トラックでズレる既知バグ）
- 初心者 / 上級者モード切り替え
- Play・Cue・ホットキュー・ループボタンのサイズ拡大
- パネル表示プリセットの保存・呼び出し
- ダークモード / ライトモード切り替え強化

---

## 実装フェーズ計画

### フェーズ 1：基盤構築（改造最小）
```
1. Windows ビルド環境構築（MSVC + vcpkg + Mixxx 2.6 フォーク）
2. Party Mix II MIDI マッピング動作確認・調整
3. ビートグリッドのタイミングオフセット修正
4. Rekordbox DB 直接インポート実装
```

### フェーズ 2：コア機能実装（C++ 改造）
```
5. 起動時間改善（非同期化・SQLite 最適化）
6. Essentia BPM / キー解析統合
7. ステム分離（Demucs v4 ONNX）＋ Party Mix II パッド Stems モード
8. YouTube 統合（URL 入力・検索・ファイル管理）
9. 波形表示改善
10. エフェクト強化（C++ 実装）
```

### フェーズ 3：機能拡張
```
11. VJ 機能（オートVJ・クロスフェード・OBS 連携）
12. Google Drive バックアップ
13. セトリ書き出し
14. スマートプレイリスト
15. UI 改善（モード切り替え・ボタンサイズ等）
```

### フェーズ 4：将来対応
```
- VST3 プラグイン対応
- OneDrive / Dropbox バックアップ
- ストリーミングサービス連携（Beatport LINK 等）
```

---

## 競合比較（RAIDJ の目標）

| 機能 | Rekordbox | Serato | Traktor | VirtualDJ | **RAIDJ** |
|---|---|---|---|---|---|
| Party Mix II 対応 | × | ○ | × | △ | **◎** |
| ステム分離 | ○（有料） | ○（有料） | ○（有料） | ○（有料） | **○（無料）** |
| VJ / オートVJ | × | × | × | ○ | **○** |
| YouTube 読み込み | × | × | × | △ | **○** |
| クラウドバックアップ | ○（有料） | × | × | × | **○** |
| セトリ書き出し | △ | △ | × | × | **○（複数形式）** |
| Rekordbox キュー移行 | — | △ | △ | △ | **◎** |
| DVS タイムコード | 有料 | 有料 | ○ | ○ | **○（無料）** |
| サブスク不要 | × | × | × | × | **◎** |

---

最終更新：2026-06-04
