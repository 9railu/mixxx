# RAIDJ 開発チェックリスト
# Mixxx 2.6 フォーク × Numark Party Mix II × Rekordbox 対応

---

## 確定済み仕様メモ

| 項目 | 決定内容 |
|---|---|
| ベース | Mixxx 2.6（ステムファイル再生対応・Qt6完全移行済み） |
| 本家追従 | 追従しない。必要なら手動マージ |
| ビルド環境 | MSVC + vcpkg（Windows） |
| ソフト名 | **RAIDJ** |
| オートミックス | なし（手動DJのみ） |
| AI次曲提案 | なし |
| エフェクト拡充 | C++直接実装 ＋ 将来的にVST3対応追加 |
| ステム分離UI | Party Mix II パッドモードに Stems モード追加（ミュート方式） |
| VJ背景映像 | 指定フォルダからランダム再生。BPMに合わせてエフェクトで調整 |
| Demucsモデル | 初回起動時に自動DL（約300MB・進捗表示あり） |
| YouTube一時ファイル | 上限10GB・超過分はソフト終了時/次回起動時に古い順削除 |
| YouTubeお気に入り | 別フォルダに永続保存 |
| YouTubeキュー管理 | 動画IDをキーにDBへ保存（再DL後も復元） |
| yt-dlp自動更新 | 起動時にバックグラウンドで確認・更新 |
| Google Drive認証 | Windows Credential Manager（DPAPI）/ Qt Keychain |
| Party Mix IIライトショー | 対応しない |

---

## 1. コントローラー対応

- [ ] Numark Party Mix II の MIDI マッピング動作確認（既存: magtomm/MIXXX-Numark-party-mix-2 を起点）
- [ ] ジョグホイールのレイテンシ調整・精度改善
- [ ] パッドの LED フィードバック修正（既知の不具合）
- [ ] 全ボタン・ノブの動作検証

### パッドモード拡張（Stems モード追加）
- [ ] 既存パッドモード切り替えサイクルに Stems モードを追加
  ```
  Hot Cues → Auto Loop → Manual Loop → Sampler → Stems（追加）
  ```
- [ ] Stems モード時のパッド割り当て実装（ミュート方式）
  - PAD 1：ドラム　ON/OFF
  - PAD 2：ベース　ON/OFF
  - PAD 3：ボーカル ON/OFF
  - PAD 4：その他　ON/OFF
- [ ] ステムミュート状態を LED の色で表示（ON=点灯 / OFF=消灯）
- [ ] Deck A / Deck B それぞれで独立して動作

## 2. Rekordbox 連携

- [ ] Rekordbox ローカル DB（master.db）からの直接インポート実装（pyrekordbox ベース）
- [ ] ホットキュー読み込み（色情報含む）
- [ ] メモリキュー読み込み
- [ ] ビートグリッド読み込み
- [ ] Rekordbox XML ファイルからのインポート対応
- [ ] USB 経由インポートの安定性確認（現行機能）
- [ ] Rekordbox タグ（My Tag）のインポート対応

## 3. 起動時間の改善

- [ ] 起動ボトルネックのプロファイリング（Qt プロファイラ使用）
- [ ] ライブラリ読み込みの非同期化（UI を先に表示してバックグラウンドでスキャン）
- [ ] SQLite クエリの最適化・インデックス追加
- [ ] 外部ライブラリ連携（Traktor / Serato / iTunes）の遅延初期化
- [ ] 起動キャッシュの実装
- [ ] yt-dlp バージョン確認・更新をバックグラウンドで実行（起動をブロックしない）
- [ ] Demucs モデル未インストール時は初回起動時に自動DL（進捗表示・約300MB）

## 4. BPM・キー解析の高精度化

- [ ] Essentia ライブラリのビルド・依存解決（FFTW, Eigen 等・vcpkg 管理）
- [ ] Essentia RhythmExtractor2013 を BPM 解析エンジンとして統合
- [ ] Essentia KeyExtractor をキー解析エンジンとして統合
- [ ] 設定画面に「解析エンジン選択」UI を追加
- [ ] 複数エンジン併用（多数決 / 信頼度スコア）による精度向上の検討
- [ ] 既存トラックの再解析機能の確認・改善
- [ ] **フレキシブル BPM グリッド対応**（変動テンポ楽曲・ロック・ディスコ等）

## 5. 波形表示の改善

- [ ] 周波数帯域別カラー表示（Rekordbox 風）の実装
- [ ] 波形の高精細化（解像度向上）
- [ ] ズームイン時の波形品質改善

## 6. ライブラリ機能の拡張

- [ ] スマートプレイリスト実装（BPM・キー・ジャンル等でフィルタ）
- [ ] 楽曲検索の高速化（大規模ライブラリ対応）

## 7. エフェクトの強化

### フェーズ1：C++ 直接実装
- [ ] 主要エフェクトの新規実装（フランジャー・フェイザー・ビットクラッシャー・リバーブ等）
- [ ] エフェクトチェーン 4 スロット対応（Traktor Pro 4 相当）
- [ ] エフェクトチェーンのプリセット保存・呼び出し
- [ ] フィルター / イコライザー / アイソレーターの品質改善
- [ ] BPM 同期エフェクト（ディレイ・フランジャー等をビートに自動同期）

### フェーズ2：VST3 対応（将来）
- [ ] VST3 プラグインホスト実装の検討
- [ ] 市販・フリー VST3 プラグインのロード・管理 UI

## 8. ステム分離（AI）

> Demucs v4 ONNX（Mixxx GSoC 2025 成果を取り込み）を使用。
> モデルは初回起動時に自動ダウンロード（約300MB）。

### モード A：事前分離＋キャッシュ（ローカル楽曲向け）
- [ ] 楽曲ロード時にバックグラウンドで Demucs v4 ONNX 分離処理
- [ ] 分離済みステムをキャッシュ保存（2回目以降は即時ロード）
- [ ] キャッシュ容量管理・古いキャッシュの自動削除設定
- [ ] オプション：LALAL.AI API キー設定で高品質10ステム分離に切り替え可能

### モード B：簡易リアルタイム分離（YouTube 楽曲向け）
- [ ] 軽量モデル（Demucs small / mobile バリアント）を使用
- [ ] ダウンロード完了後すぐに分離処理開始（vocal / instrumental の2ステム）
- [ ] 処理完了後はキャッシュ保存してモード A と統一管理

### 共通
- [ ] ステム別ボリューム / フィルター / エフェクト制御
- [ ] Party Mix II パッドの Stems モードと連動（→ 1. 参照）

## 9. YouTube 統合

> 個人・自宅利用前提。Mixxx 本体への組み込み方式。
> 内部処理：yt-dlp → ffmpeg → デッキへ直接ロード。

### 9-1. URL 直接入力
- [ ] ライブラリパネルに「YouTube」セクションを追加
- [ ] URL 入力フィールド（youtube.com / youtu.be 両形式対応）
- [ ] URL 貼り付け → メタデータ（タイトル・長さ・サムネイル）プレビュー表示
- [ ] デッキ 1 / デッキ 2 への直接ロードボタン
- [ ] 変換中のプログレスバー表示

### 9-2. YouTube 検索
- [ ] キーワード検索フィールド
- [ ] yt-dlp の検索機能で結果取得（API キー不要）
- [ ] 検索結果一覧（タイトル・チャンネル・長さ・サムネイル）
- [ ] ワンクリックでデッキにロード
- [ ] 検索履歴の保存

### 9-3. ファイル管理
- [ ] 一時ファイル上限：10GB（超過分はソフト終了時 / 次回起動時に古い順削除）
- [ ] **お気に入りボタン**：押したものは別フォルダに永続保存・削除対象外
- [ ] YouTube 動画 ID をキーに mixxxdb へキュー・ホットキューを保存
- [ ] 再ダウンロード時に同じ動画 ID であればキュー自動復元
- [ ] ローカルキャッシュ済みかどうかをライブラリ上で表示

### 9-4. 共通
- [ ] BPM・キーは取得後 Essentia エンジンで自動解析
- [ ] yt-dlp 自動更新：起動時にバックグラウンドで確認・差異があれば更新

## 10. 楽曲バックアップシステム（Google Drive）

### 10-1. Google Drive 連携
- [ ] OAuth 2.0 認証実装
- [ ] 認証トークンを Windows Credential Manager（DPAPI）/ Qt Keychain で安全保存
- [ ] バックアップ対象の選択 UI
- [ ] 手動バックアップ実行ボタン
- [ ] 自動バックアップ（起動時 / 終了時 / 定期）の設定
- [ ] 差分バックアップ対応（変更ファイルのみ転送）
- [ ] バックアップ進捗表示・ログ
- [ ] Google Drive からのリストア機能
- [ ] バックアップ履歴一覧と世代管理

### 10-2. バックアップ対象
- [ ] Mixxx ライブラリDB（mixxxdb.sqlite）
- [ ] キュー・ホットキュー・ビートグリッドデータ
- [ ] YouTube キャッシュDB（動画ID＋キュー情報）
- [ ] コントローラーマッピングファイル
- [ ] 設定ファイル（mixxx.cfg）
- [ ] 楽曲ファイル本体（任意選択）

### 10-3. 拡張ストレージ対応（将来）
- [ ] OneDrive / Dropbox 対応の検討
- [ ] ローカル NAS / 外部ドライブへのバックアップ対応

## 11. セトリ（セットリスト）書き出し機能

### 11-1. セトリ記録

#### 全体制御
- [ ] セトリ機能自体の ON / OFF トグル

#### 自動記録モード
- [ ] 再生開始と同時に自動記録開始
- [ ] デッキにロードされた曲を順次追記

#### 手動記録モード（自動記録と並行動作）
- [ ] 手動記録ボタンを押した時点から記録開始（トグル式）
- [ ] 手動記録中はインジケーター表示
- [ ] 自動・手動は独立したセトリとして同時保持
- [ ] 書き出し時にどちらを使うか選択可能

#### 共通
- [ ] 記録内容：曲名・アーティスト・BPM・キー・再生開始時刻・再生時間
- [ ] 手動での曲追加・削除・並び替え
- [ ] セッション管理（日付・イベント名を付けて保存）

### 11-2. 書き出し形式
- [ ] テキスト（.txt）
- [ ] CSV（.csv）
- [ ] JSON（.json）
- [ ] 画像（.png）：SNS シェア用カード形式

### 11-3. 共有
- [ ] クリップボードへのワンクリックコピー
- [ ] Google ドライブへの自動保存
- [ ] Twitter / X 向けフォーマット（文字数制限対応）

## 12. VJ機能（オートVJ）

> DJプレイをするだけで自動的にビジュアルが動く設計。VJ側を手動操作しなくてよい。

### 12-1. オーディオ連動の自動ビジュアル
- [ ] BPM をビジュアルアニメーション速度に自動同期
- [ ] FFT 周波数分析 → 低音/中音/高音エネルギーをリアルタイム取得
- [ ] 周波数エネルギーをビジュアルパラメータに反映（サイズ・輝度・色等）
- [ ] クロスフェーダー位置を映像クロスフェード率に自動連動
- [ ] ホットキュー押下 → フラッシュ・色変化・エフェクト自動発動
- [ ] ループ中は映像もループエフェクト自動適用

### 12-2. 自動シーン切り替え
- [ ] ドロップ検出（低音量→突然大音量）→ 激しいビジュアルへ自動遷移
- [ ] ブレイク検出（静かな区間）→ 落ち着いたビジュアルへ自動遷移
- [ ] エネルギーレベルに応じて GL Transitions 121種から自動選択
  - 高エネルギー → 激しいトランジション（Flash・Glitch 等）
  - 低エネルギー → なめらかなトランジション（Fade・Wipe 等）
- [ ] BPM に合わせてエフェクト強度・速度を自動調整（映像が音楽に馴染むよう補正）

### 12-3. 映像素材の自動管理
- [ ] YouTube 楽曲ロード時 → その映像を VJ 素材として自動使用
- [ ] ローカル楽曲ロード時 → 指定フォルダからランダム再生
- [ ] 背景映像フォルダの登録 UI（複数フォルダ対応）

### 12-4. 映像クロスフェード（FFmpeg xfade + GL Transitions）
- [ ] FFmpeg libavfilter xfade フィルターグラフの実装
- [ ] GL Transitions（MIT・121種）GLSL シェーダーの組み込み
- [ ] 音声クロスフェードに映像クロスフェードを自動追従

### 12-5. VJ 出力・配信連携
- [ ] VJ 出力ウィンドウ（外部ディスプレイ / プロジェクター向け）
- [ ] NDI SDK 組み込み → 映像＋音声を OBS へ送出
- [ ] OBS WebSocket 連携 → 曲名・BPM・キーを OBS テキストソースに自動反映
- [ ] 音声のみ連携：VB-Audio VirtualCable 設定ガイドをアプリ内に表示

### 12-6. 手動調整（必要なときだけ触れる）
- [ ] VJ ON / OFF トグル
- [ ] トランジション種別の手動上書き
- [ ] 背景映像の手動切り替えボタン
- [ ] エフェクト強度スライダー（デフォルトは自動）

## 13. UI・操作性の改善

- [ ] **ビートグリッドのタイミングオフセット修正**（全トラックで若干前にズレる既知バグ）
- [ ] **初心者 / 上級者モード切り替え**
- [ ] **ボタンサイズの拡大**（Play・Cue・ホットキュー・ループ）
- [ ] パネルの表示/非表示プリセットの保存・呼び出し
- [ ] ダークモード / ライトモードの切り替え強化

## 14. ストリーミングサービス連携（将来参考）

- [ ] Beatport LINK 連携の検討
- [ ] SoundCloud GO 連携の検討
- [ ] TIDAL 連携の検討

## 15. その他・品質改善

- [ ] Windows（MSVC + vcpkg）ビルド手順の確立・ドキュメント化
- [ ] GitHub Copilot CLI を使った開発フロー整備
- [ ] 各改善項目のユニットテスト追加
- [ ] 実機（Party Mix II）での統合テスト
- [ ] ライブラリスキャン中の UI 固まり解消
- [ ] 大規模ライブラリ時のスクロール・検索パフォーマンス改善

---

## 優先度・フェーズ計画

### フェーズ 1（改造不要 or 最小改造）
| 優先度 | 項目 |
|---|---|
| 最高 | Windows ビルド環境構築（MSVC + vcpkg） |
| 最高 | コントローラー MIDI マッピング動作確認・調整 |
| 高 | ビートグリッドのタイミングオフセット修正 |
| 高 | Rekordbox DB 直接インポート |

### フェーズ 2（C++ コア改造）
| 優先度 | 項目 |
|---|---|
| 高 | 起動時間改善（非同期化） |
| 高 | Essentia BPM / キー解析統合 |
| 高 | ステム分離（Demucs v4 ONNX）＋ パッドUI |
| 中 | YouTube 統合（URL入力・検索・ファイル管理） |
| 中 | 波形表示改善 |
| 中 | エフェクト強化（C++実装） |

### フェーズ 3（機能拡張）
| 優先度 | 項目 |
|---|---|
| 中 | VJ 機能（オートVJ・クロスフェード・OBS連携） |
| 中 | Google Drive バックアップ |
| 中 | セトリ書き出し |
| 中 | スマートプレイリスト |
| 低 | VST3 対応 |
| 低 | ストリーミングサービス連携 |

---

## 他社ソフト比較サマリー（参考）

| 機能 | Rekordbox | Serato | Traktor | VirtualDJ | **RAIDJ（目標）** |
|---|---|---|---|---|---|
| ステム分離 | ○ | ○ | ○（4stem） | ○（5stem） | **○（Demucs）** |
| フレキシブル BPM | △ | △ | ○ | ○ | **○** |
| VJ / 映像出力 | × | × | × | ○ | **○（オートVJ）** |
| YouTube 統合 | × | × | × | △ | **○** |
| クラウドバックアップ | ○（有料） | × | × | × | **○（Google Drive）** |
| セトリ書き出し | △ | △ | × | × | **○（複数形式）** |
| DVS タイムコード | 有料 | 有料 | ○ | ○ | **○（無料）** |
| サブスク不要 | × | × | × | × | **○** |

---

最終更新：2026-06-04（全決定事項反映・RAIDJ として確定）
